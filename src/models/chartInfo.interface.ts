export interface ChartInfo {
    type        : string;
    unit        : string;
    lineColor   : string;
    chartView   : any;
}
export interface ChartOptions {
    pollutant: string;
    unit: string;
    color: string;
}